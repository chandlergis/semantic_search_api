from typing import Tuple
import hashlib

def process_uploaded_file(file_content: bytes) -> Tuple[str, str]:
    """Process uploaded file and return (content_hash, markdown_content)"""
    pass

def chunk_text(text: str, chunk_size: int = 1000) -> list[str]:
    """Split text into chunks of specified size"""
    pass
